

------

# 📘 考研时间复杂度分析速查表

------

## 一、基本步骤

1. 找到最内层的基本操作（如 `sum++`）
2. 计算该语句的执行次数 `T(n)`
3. 保留最高阶项并去掉常数系数
4. 得到时间复杂度：`O(f(n))`

------

## 二、常见循环规律

| 代码结构                    | 执行次数 | 时间复杂度     |
| --------------------------- | -------- | -------------- |
| `for(i=0;i<n;i++)`          | n        | **O(n)**       |
| `for(i=1;i<n;i*=2)`         | log n    | **O(log n)**   |
| 双层 `for(i,j<n)`           | n²       | **O(n²)**      |
| `for(i<n;i++) for(j<i;j++)` | n²/2     | **O(n²)**      |
| 外层 `i*=2`，内层 `j<n`     | n·log n  | **O(n log n)** |
| 外层 `i*=2`，内层 `j<i`     | n        | **O(n)**       |

------

## 三、while 循环分析

| 代码示例           | 执行次数 | 时间复杂度   |
| ------------------ | -------- | ------------ |
| `while(i<n) i++;`  | n        | **O(n)**     |
| `while(i<n) i*=2;` | log n    | **O(log n)** |

------

## 四、递归时间复杂度

| 递归形式               | 递推式              | 时间复杂度     |
| ---------------------- | ------------------- | -------------- |
| 线性递归               | `T(n)=T(n-1)+O(1)`  | **O(n)**       |
| 二分递归               | `T(n)=T(n/2)+O(1)`  | **O(log n)**   |
| 分治递归（如归并排序） | `T(n)=2T(n/2)+O(n)` | **O(n log n)** |

------

## 五、主定理（Master Theorem）

形式：
[
T(n) = aT(n/b) + O(n^d)
]

| 情况         | 条件    | 结果               |
| ------------ | ------- | ------------------ |
| 子问题增长慢 | a < b^d | **O(n^d)**         |
| 平衡         | a = b^d | **O(n^d log n)**   |
| 子问题增长快 | a > b^d | **O(n^{log_b a})** |

------

## 六、典型代码与复杂度对照表

| 代码结构                    | 时间复杂度 |
| --------------------------- | ---------- |
| `for(i=0;i<n;i++)`          | O(n)       |
| `for(i=1;i<n;i*=2)`         | O(log n)   |
| 双层 `for(i,j<n)`           | O(n²)      |
| `for(i<n;i++) for(j<i;j++)` | O(n²)      |
| 外层 `i*=2`, 内层 `j<n`     | O(n log n) |
| 外层 `i*=2`, 内层 `j<i`     | O(n)       |
| `T(n)=T(n-1)+1`             | O(n)       |
| `T(n)=T(n/2)+1`             | O(log n)   |
| `T(n)=2T(n/2)+n`            | O(n log n) |

------

## 七、考试技巧总结

✅ **看增长方式：**

- “+” → 线性复杂度
- “×” → 对数复杂度

✅ **看嵌套：**

- 总复杂度 ≈ 外层 × 内层

✅ **看依赖关系：**

- 内层依赖外层变量时 → 求和计算

✅ **看递归：**

- 套主定理（Master Theorem）快速判断

✅ **忽略常数与低阶项：**

- 如 n/2、n−1 都视为 O(n)

------

是否希望我帮你把这个 Markdown 转成 `.md` 文件方便下载？