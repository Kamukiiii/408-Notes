

------

#  操作系统与计算机组成原理整合笔记

```
宏观上：
程序最初存放在外存（磁盘）中，当操作系统调度它运行时，会为其分配内存空间、建立进程控制块（PCB），从而形成进程。
当系统内存不足时，操作系统可以将暂时不用的进程挂起并换出到外存（后备队列），等需要运行时再换入内存，这个过程体现了进程的挂起/激活机制。

微观上：
OS将某个进程的逻辑地址空间划分为一个个页，又将内存，即物理地址划分为一个个和页大小相同的页框，便于将页调入主存中的页框之中。然而直接从外存映射到内存的速度太慢了，因此快表和页表应需而生。

页表记录了虚拟页号与物理页框号之间的映射关系，相当于地址翻译的“索引表”，并不存放数据内容。如果在页表中能查到当前待调入页的页框号，则直接映射为物理地址即可。但是页表就在主存中，还是需要访问主存，为了提高效率，出现了TLB快表。

TLB相当于页表的高速缓存，它位于CPU内部的存储器管理单元（MMU）中，存放最近访问的页表项。查TLB命中即可直接得到物理页框号，未命中则访问内存中的页表，根据空间局部性和时间局部性，短时间内某个进程是非常有可能被多次访问的，TLB就是利用了这一点。

当访问的页不在内存中时，会触发缺页中断，操作系统根据页面置换算法（如LRU、FIFO）选择一个物理页框换出，从外存调入所需页面。

以上是操作系统的内容

接下来是计算机组成原理的内容
CPU的功能可以概括为取指令->分析指令->执行指令
指令从何而来，根据指令格式可以直到，我们要获得操作数是需要寻址的，不同的寻址方式获得操作数的方式就不同。操作数既可能来自主存，也可能来自寄存器或立即数。CPU根据寻址方式解析出有效地址（EA），然后通过总线访问对应存储单元。
主存中存放的是数据和指令，它们被映射到物理地址空间。虚拟地址是CPU在执行程序时看到的地址，它需要通过OS的分页机制转换成物理地址后才能访问主存。
CPU通过地址码寻址，地址码对应着主存中的物理地址。但是CPU太快了，主存又太慢了，它们两之间的速度是不均衡的，这会导致CPU可能发送了一大堆寻址要求，但是主存处理不过来。所以产生了cache这一高速存储器。Cache就像一个缓冲池，缓和CPU和主存的速度，所以cache中放的其实就是主存中的“复印件”，那它们之间该如何映射呢？这就是三种映射方式的原理：直接映射、全相联映射和组相联映射。

在指令执行的取指阶段，CPU 首先发出一个虚拟地址（Virtual Address，VA）。该虚拟地址被送到 存储器管理单元（MMU），由其负责通过 页表和快表（TLB） 将其翻译成对应的 物理地址（Physical Address，PA）。

随后，CPU将这个物理地址通过地址总线发送到Cache（高速缓存）进行查找。如果 Cache 命中（Hit），则指令直接从 Cache 调入 CPU；如果 Cache 未命中（Miss），则需要通过地址总线访问主存（Main Memory）。主存根据物理地址寻址，将对应的指令（或数据）通过数据总线送回给 CPU，首先写入 Cache，然后再加载到 CPU 内部的指令寄存器（IR） 中，从而完成取指令的操作。

再细化：主存的寻址
主存的寻址，本质上就是将 CPU 发来的一个物理地址，通过内存控制器，拆分成行地址和列地址，利用硬件电路在巨大的 DRAM 存储阵列中精确地定位到所需的存储单元，最终完成数据的读取或写入操作。
现代主存为了提高速度，通常会采用多模块交叉存取技术。这意味着主存被分成几个独立的存储体。地址的高位可能用于选择存储体。地址的低位则用于选择存储体内的单元。通过让相邻的地址分布在不同的存储体中，可以实现并行访问，极大地提高内存的访问速度。


上述过程可以说就是计算机组成原理在讨论的核心内容了。
```

------

## 一、宏观视角：操作系统的管理与调度

### 1. 进程与存储层次

- 外存（磁盘）中存放的是 **程序或进程映像**。
- 当操作系统调度某个程序运行时，会：
  - 为其分配内存空间；
  - 建立进程控制块（PCB）；
  - 从外存将程序调入内存，形成真正运行的**进程**。

> 当内存不足时，系统会将暂时不用的进程**挂起**，换出到外存的后备队列；
> 当该进程需要再次运行时，再换入内存恢复运行。
> 这就是进程的**驻留与非驻留机制**。

#### 🌀 进程状态转换（核心）

```
就绪 ↔ 运行 ↔ 阻塞
 ↑                   ↓
后备（外存） ←→ 挂起
```

------

## 二、微观视角：虚拟内存与页表机制

### 1. 虚拟地址与物理地址

操作系统将每个进程的**逻辑地址空间**划分为多个**页（Page）**，
将主存（物理内存）划分为大小相同的**页框（Frame）**，
以此实现按页调度与地址映射。

> 目的：让每个进程“感觉”自己独占内存，同时高效利用物理空间。

------

### 2. 页表（Page Table）

页表记录：

```
虚拟页号 → 物理页框号
```

页表项中包含：

- 页框号（Frame Number）
- 有效位（Valid Bit）
- 访问位 / 修改位（Access / Dirty Bit）

页表本身存在于主存中，因此访问时要经历：

```
CPU → 内存（查页表） → 再次访问内存（取数据）
```

这就引出了快表（TLB）。

------

### 3. TLB（快表 / Translation Lookaside Buffer）

- 是页表项的**高速缓存**，位于CPU内部或与MMU集成。
- 存放最近访问的页表项。
- 利用**时间局部性**与**空间局部性**提高地址转换速度。

#### 地址转换过程：

```
CPU发出虚拟地址 → 查TLB
     ↓命中
  直接得出物理页框号
     ↓未命中
  查主存中的页表 → 更新TLB
```

------

### 4. 页面置换（Page Replacement）

- 若访问的页不在内存中，会触发 **缺页中断**。
- 操作系统会根据算法（如 FIFO、LRU、OPT）：
  - 选择一个物理页框换出；
  - 从外存调入所需页面；
  - 更新页表与TLB。

#### 🔁 总结

| 机制     | 解决的问题               |
| -------- | ------------------------ |
| 虚拟内存 | 内存容量有限             |
| 页表     | 逻辑地址到物理地址的映射 |
| TLB      | 地址转换速度慢           |
| 页面置换 | 内存空间不足             |

------

## 三、计算机组成原理：执行层的实现机制

### 1. CPU的基本功能

> **取指令 → 分析指令 → 执行指令**

CPU控制整个计算机系统的操作，核心组成：

- **CU（控制单元）**：负责指令译码与控制信号生成
- **ALU（算术逻辑单元）**：执行运算操作
- **寄存器组**：暂存操作数与结果
- **总线系统**：连接CPU、主存和I/O设备

------

### 2. 指令执行过程

#### (1) 取指阶段（Fetch）

- CPU通过**地址总线**发送指令地址；
- 主存接收到读信号，通过**数据总线**返回指令；
- 指令被送入**指令寄存器（IR）**。

#### (2) 分析阶段（Decode）

- 控制单元CU解析指令操作码与寻址方式；
- 确定操作数位置（寄存器、主存、立即数等）。

#### (3) 执行阶段（Execute）

- ALU完成指令所需运算；
- 执行结果写回寄存器或主存。

------

### 3. 寻址与主存访问

- 寻址方式决定了操作数的来源与访问方式；
- CPU通过地址码计算**有效地址（EA）**；
- 主存根据地址通过总线系统返回数据。

> **主存存放的就是“由虚拟地址映射得到的物理地址内容”。**

------

### 4. Cache：缓解主存与CPU速度差

由于CPU太快、主存太慢，二者速度不匹配。
因此引入**Cache高速缓存**，存放主存中近期访问的数据副本。

#### 三种映射方式

| 映射方式   | 特点               |
| ---------- | ------------------ |
| 直接映射   | 简单快速，冲突多   |
| 全相联映射 | 命中率高，硬件复杂 |
| 组相联映射 | 折中方案（常用）   |

Cache命中 → 直接读取
未命中 → 访问主存，并根据替换策略（如LRU）更新Cache。

------

### 5. 三总线结构（信号流向）

```
┌──────────┐
│   CPU    │
└──────────┘
   ↑ ↓
地址总线：传地址
数据总线：传数据
控制总线：传命令与时序信号
   ↑ ↓
┌──────────┐
│   主存    │
└──────────┘
```

------

## 四、从系统到硬件的协同逻辑

| 层级      | 代表机制           | 解决问题           |
| --------- | ------------------ | ------------------ |
| 操作系统  | 虚拟内存、进程调度 | 程序的抽象与管理   |
| MMU / TLB | 地址映射与转换     | 虚拟地址转物理地址 |
| Cache     | 高速缓存机制       | 速度差异           |
| 主存      | 实际存储空间       | 程序与数据存放     |
| 外存      | 后备与交换空间     | 持久存储           |

------

## 🧩 五、总结：从宏观到微观的统一视角

> 从宏观上看，操作系统通过进程管理和虚拟内存机制，实现了对程序的统一调度与内存抽象；
> 从微观上看，计算机组成原理解释了CPU如何通过层层存储体系（Cache、主存、外存）与寻址机制来实现指令的高效执行。
>
> 两者相辅相成，前者解决“**管理与调度**”的问题，后者解决“**执行与实现**”的问题。

------

## 📘 附图：系统层次结构示意

```
          ┌────────────┐
          │   应用程序  │
          ├────────────┤
          │ 操作系统(OS)│
          ├────────────┤
          │   虚拟内存  │
          ├────────────┤
          │  主存 (RAM) │
          ├────────────┤
          │   Cache     │
          ├────────────┤
          │   CPU/MMU   │
          ├────────────┤
          │   外存(磁盘)│
          └────────────┘
```

```
┌──────────────────────────────┐
│        外存（磁盘）          │
│  存放程序/进程映像文件       │
└────────────┬───────────────┘
             │ ① 调入
             ▼
┌──────────────────────────────┐
│   操作系统创建进程 (PCB)      │
│   为程序分配内存空间          │
│   虚拟地址空间划分为“页”     │
└────────────┬───────────────┘
             │ ② 虚拟内存机制
             ▼
┌──────────────────────────────┐
│    页表 (Page Table)          │
│  记录“虚拟页号 → 物理页框号” │
│  存放在主存中                 │
└────────────┬───────────────┘
             │ ③ 地址转换
             ▼
┌──────────────────────────────┐
│   TLB（快表）                 │
│   最近访问的页表项缓存        │
│   命中 → 得物理地址           │
│   未命中 → 查页表 / 换页      │
└────────────┬───────────────┘
             │ ④ 缺页处理（必要时）
             ▼
┌──────────────────────────────┐
│     主存（物理内存）          │
│   存放当前活跃页与数据        │
└────────────┬───────────────┘
             │ ⑤ CPU取指
             ▼
┌──────────────────────────────┐
│   Cache（高速缓存）           │
│  存放主存中最近访问的数据副本 │
│  命中 → 直接访问              │
│  未命中 → 访问主存更新Cache   │
└────────────┬───────────────┘
             │ ⑥ 数据返回
             ▼
┌──────────────────────────────┐
│   CPU（CU + ALU + 寄存器）   │
│  CU译码指令 → ALU执行 → 写回 │
│  地址总线：发地址             │
│  数据总线：收数据             │
│  控制总线：发控制信号         │
└────────────┬───────────────┘
             │ ⑦ 执行结果
             ▼
┌──────────────────────────────┐
│     操作系统更新状态 / 调度   │
│     可能换出进程或页面        │
└──────────────────────────────┘

```

图风格（适合打印或复习）。